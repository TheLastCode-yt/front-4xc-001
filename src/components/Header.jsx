import React from 'react';

const Header = () => {
  const links = [{ name: 'Negociação', url: '/' }];

  return (
    <header>
      <div className="w-screen bg-white p-10">
        <div className="container mx-auto"></div>
      </div>
    </header>
  );
};

export default Header;
